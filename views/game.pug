doctype html
html
  head
    title Card Matching Game
    link(rel="stylesheet" href="/styles.css")
  body
    h1 Welcome #{user.username}
    p Your high score: #{user.highScore}
    //- Game grid and logic go here
    form(action=`/game/${user.username}/play` method="POST")
      label(for="score") Enter your score:
      input(type="number" name="score" required)
      button(type="submit") Submit Score
    a(href="/") Back to Home